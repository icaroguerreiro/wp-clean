{% extends '_template.twig' %}

{% block content %}

  {% for item in posts %}
    <h1><a href="{{item.link}}">{{item.title}}</a></h1>
    {{item.get_preview}}
  {% endfor %}

  <p>Eitaaaaaa</p>

  {% for post in testando %}
    <h2>{{ post.title }}</h2>
  {% endfor %}

  <footer class="pagination">
    {% if pagination.prev %}
      <a href="{{pagination.prev.link}}">Newer</a>
    {% endif %}

    {% if pagination.next %}
      <a href="{{pagination.next.link}}">Older</a>
    {% endif %}
  </footer>

{% endblock %}
